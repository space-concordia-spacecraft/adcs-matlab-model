To run the simulation:

	1. Open matlab in this folder
	2. open ADCS_Outline.slx
	3. run the simulation from simulink.

You can modify the parameters in SC_ODIN_INITIALIZATION.m

Every other file is important/used! *Do not delete anything*

The STK_SPOOKY folder contains the STK simulation that match our TLE and Targets.